/*
 * Copyright (c)
 *        @author Timothé PARDIEU
 *                 ${PACKAGE_NAME}
 *                 Created on - ${DATE} (${TIME})
 *                 Build for project ${PROJECT_NAME}
 *
 */

package test.upmc.pcg.game.card;

import org.junit.After;
import org.junit.Before;
import org.junit.Test;

/**
 * TrainerCard Tester.
 *
 * @author <Authors name>
 * @version 1.0
 * @since <pre>d?c. 9, 2017</pre>
 */
public class TrainerCardTest {

    @Before
    public void before() throws Exception {
    }

    @After
    public void after() throws Exception {
    }

    /**
     * Method: createCard(ArrayList arrayList, Player currentPlayer)
     */
    @Test
    public void testCreateCard() throws Exception {
    }

    /**
     * Method: updateCardFromCollec(ArrayList alUpdate)
     */
    @Test
    public void testUpdateCardFromCollec() throws Exception {
    }

    /**
     * Method: toString()
     */
    @Test
    public void testToString() throws Exception {
    }


    /**
     * Method: generateCard(String cardTitle, int idCard, TrainerType trainerType, String description)
     */
    @Test
    public void testGenerateCard() throws Exception {
/*
try { 
   Method method = TrainerCard.getClass().getMethod("generateCard", String.class, int.class, TrainerType.class, String.class); 
   method.setAccessible(true); 
   method.invoke(<Object>, <Parameters>); 
} catch(NoSuchMethodException e) { 
} catch(IllegalAccessException e) { 
} catch(InvocationTargetException e) { 
} 
*/
    }

} 
